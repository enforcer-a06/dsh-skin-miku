# dsh-skin-miku bundle patch — insert one loader entry for the plugin
# package. The entry is a normal cordis loader entry (id + package name); the
# browser half is picked up by dsh-client-modules through the package's
# dsh.client declaration, exactly like dsh-skin / the shipped ui-* packages.
- insert:
    - id: skin-miku
      name: 'dsh-skin-miku'
